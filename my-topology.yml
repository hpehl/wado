name: my-topology
version: 39
hosts:
  - name: dc
    domain-controller: true
  - servers:
      - name: server-one
        auto-start: true
      - name: server-two
        offset: 10
      - name: server-three
        group: other-server-group
        offset: 20
      - name: server-four
        group: other-server-group
        offset: 30
  - name: host
    servers:
      - name: server-one
      - name: server-two
        group: osg
      - name: server-three
        group: osg
  - version: 38
    servers:
      - name: server-one
      - name: server-two
      - name: server-three
        group: osg
